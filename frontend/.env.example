# URL of your deployed FastAPI backend (no trailing slash, no /api/v1 suffix).
# Locally, this is usually http://localhost:8000 when running:
#   uvicorn backend.main:app --reload
VITE_API_URL=http://localhost:8000
